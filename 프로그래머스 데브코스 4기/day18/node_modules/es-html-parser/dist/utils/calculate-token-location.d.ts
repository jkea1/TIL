import { Range } from "../types";
export declare function calculateTokenLocation(source: string, range: Range): {
    start: {
        line: number;
        column: number;
    };
    end: {
        line: number;
        column: number;
    };
};
