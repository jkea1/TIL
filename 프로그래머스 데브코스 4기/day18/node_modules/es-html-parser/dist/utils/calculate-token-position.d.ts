import { TokenizerState } from "../types";
export declare function calculateTokenPosition(state: TokenizerState, options: {
    keepBuffer: boolean;
}): {
    range: import("../types").Range;
    loc: {
        start: {
            line: number;
            column: number;
        };
        end: {
            line: number;
            column: number;
        };
    };
};
