data type
자료형

1. 숫자형
2. BigInt
  : 숫자 중에 가장 큰 수를 나타낸다.
  : 정수 리터럴 끝에 n을 붙이면 BigInt로 분리된다. 
    ex)
    let BigInt = 123n;
3. 문자형
  : 문자열을 따옴표로 묶거나 벡틱을 이용해 나타낼 수 있다. 
    ex)
    let string = 'name';
    let string1 = `name1`;
4. 불린형
  : true/false
5. null값
  : 앞의 어느 자료형에도 속하지 않은 값이다. 
  : 값을 비워두는 형태로 사용 하기도 한다. 
  ex)
  let age = null;
6. undefined 값
  : 값이 할당되지 않은 상태를 나타낸다. 

  ❗️ undefined vs null 
    아직 값을 할당한 상태가 아니면 undefined 한 상태가 맞고, 개발자가 고의로 값을 비워뒀다면 null을 사용하는 것이 맞다.

7. 객체 

8. 심볼

  ❗️typeof
    : typeof 이용해 자료형의 종류를 알 수 있다. 
      1. 연산자로의 방식
        typeof ...
      2. 함수로의 방식
        : 연산의 우선순위를 놓여야 할때 사용
        typeof(...)
          ex) typeof(1+2);
      
      ⭐️ typeof null의 결과는 "object"이다. 
        -> null은 별도의 고유한 자료형을 가지는 특수 값으로 객체가 아니지만, 하위 호환성을 유지하기 위해 이런 오류를 수정하지 않고 남겨둔 상황이다. 